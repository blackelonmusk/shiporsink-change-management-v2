/** @type {import('tailwindcss').Config} */
module.exports = {
  content: [
    './pages/**/*.{js,ts,jsx,tsx,mdx}',
    './components/**/*.{js,ts,jsx,tsx,mdx}',
    './app/**/*.{js,ts,jsx,tsx,mdx}',
  ],
  theme: {
    extend: {
      colors: {
        // Orange accent (Change Management)
        primary: '#f97316',
        secondary: '#ea580c',
        
        // Zinc scale (Suite standard dark theme)
        zinc: {
          950: '#0a0a0a',  // Page background
          900: '#18181b',  // Cards, panels
          800: '#27272a',  // Elevated surfaces, borders
          700: '#3f3f46',  // Hover states
          600: '#52525b',  // Active borders
          500: '#71717a',  // Muted text
          400: '#a1a1aa',  // Secondary text
          300: '#d4d4d8',  // Primary text alt
          200: '#e4e4e7',
          100: '#f4f4f5',
          50: '#fafafa',
        },
        
        // App accent colors (for Suite Apps component)
        'app-orange': '#f97316',
        'app-purple': '#a855f7',
        'app-cyan': '#06b6d4',
        'app-emerald': '#10b981',
        
        // Status colors
        success: '#10b981',
        warning: '#f59e0b',
        error: '#ef4444',
      },
      
      backgroundColor: {
        // Quick access to common backgrounds
        'page': '#0a0a0a',
        'card': '#18181b',
        'elevated': '#27272a',
      },
      
      borderColor: {
        'default': '#27272a',
        'hover': '#3f3f46',
        'active': '#52525b',
      },
      
      boxShadow: {
        'glow-orange': '0 0 30px rgba(249, 115, 22, 0.3)',
        'glow-orange-lg': '0 0 50px rgba(249, 115, 22, 0.4)',
        'card': '0 10px 40px rgba(0, 0, 0, 0.2)',
        'card-hover': '0 20px 50px rgba(0, 0, 0, 0.3)',
      },
      
      keyframes: {
        fadeIn: {
          '0%': { opacity: '0' },
          '100%': { opacity: '1' },
        },
        slideUp: {
          '0%': { transform: 'translateY(20px)', opacity: '0' },
          '100%': { transform: 'translateY(0)', opacity: '1' },
        },
        slideDown: {
          '0%': { transform: 'translateY(-10px)', opacity: '0' },
          '100%': { transform: 'translateY(0)', opacity: '1' },
        },
        scaleIn: {
          '0%': { transform: 'scale(0.95)', opacity: '0' },
          '100%': { transform: 'scale(1)', opacity: '1' },
        },
        shimmer: {
          '0%': { transform: 'translateX(-100%)' },
          '100%': { transform: 'translateX(100%)' },
        },
        pulseGlow: {
          '0%, 100%': { boxShadow: '0 0 20px rgba(249, 115, 22, 0.4)' },
          '50%': { boxShadow: '0 0 40px rgba(249, 115, 22, 0.7)' },
        },
        float: {
          '0%, 100%': { transform: 'translateY(0)' },
          '50%': { transform: 'translateY(-10px)' },
        },
        bounce: {
          '0%, 100%': { transform: 'translateY(0)' },
          '50%': { transform: 'translateY(-5px)' },
        },
      },
      
      animation: {
        'fadeIn': 'fadeIn 0.3s ease-out',
        'slideUp': 'slideUp 0.4s ease-out',
        'slideDown': 'slideDown 0.3s ease-out',
        'scaleIn': 'scaleIn 0.2s ease-out',
        'shimmer': 'shimmer 2s infinite',
        'pulse-glow': 'pulseGlow 2s ease-in-out infinite',
        'float': 'float 3s ease-in-out infinite',
        'bounce-soft': 'bounce 1s ease-in-out infinite',
      },
      
      fontFamily: {
        sans: ['Inter', 'system-ui', '-apple-system', 'sans-serif'],
        mono: ['JetBrains Mono', 'monospace'],
      },
      
      borderRadius: {
        'xl': '0.75rem',
        '2xl': '1rem',
      },
      
      transitionDuration: {
        '150': '150ms',
        '200': '200ms',
      },
    },
  },
  plugins: [],
}
